# Core Django
# Settings/codebase expect Django 5.2.x (see `noctis_pro/settings.py` header).
Django==5.2.5
Pillow
django-widget-tweaks
python-dotenv

# ASGI / WebSockets (required by INSTALLED_APPS: daphne, channels)
daphne
channels

# Production server
gunicorn
whitenoise

# Database
psycopg2-binary
dj-database-url

# Redis and caching
redis
django-redis

# API
djangorestframework
django-cors-headers
requests

# DICOM processing
pydicom
pynetdicom
highdicom
numpy

# Pixel data decoders (required for compressed DICOM: JPEG/JPEG2000/RLE)
pylibjpeg
pylibjpeg-libjpeg
pylibjpeg-openjpeg
pylibjpeg-rle

# Background tasks (requires more memory)
celery

# Advanced image processing
opencv-python
scikit-image
matplotlib

# AI and machine learning (high memory requirement)
scipy
pandas
torch
torchvision
scikit-learn
