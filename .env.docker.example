## Noctis Pro - Docker Compose production env
## Copy to .env and edit values:
##   cp .env.docker.example .env

# Django
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_VALUE
ALLOWED_HOSTS=*

# Postgres (persistent via Docker named volume)
POSTGRES_DB=noctispro
POSTGRES_USER=noctispro
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ngrok
NGROK_AUTHTOKEN=PASTE_YOUR_NGROK_AUTHTOKEN
# Optional reserved domain (recommended for stable URL):
# NGROK_DOMAIN=yourname.ngrok.app

