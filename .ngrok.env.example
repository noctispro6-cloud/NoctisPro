# Local-only ngrok settings (copy to `.ngrok.env`).
#
# IMPORTANT:
# - Do NOT commit `.ngrok.env` (it's already gitignored in this repo)
# - Put your real token here
NGROK_AUTHTOKEN=PUT_YOUR_NGROK_TOKEN_HERE

# Optional: use a reserved ngrok domain (requires it to exist in your ngrok account)
NGROK_DOMAIN=noctis-pro.com.ngrok.app

